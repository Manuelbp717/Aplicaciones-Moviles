import 'package:flutter/material.dart';

class PanCakeTab extends StatelessWidget {
  const PanCakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("PanCakeTab"))
    );
  }
}